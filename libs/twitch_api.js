const api = require('twitch-api-v5');

api.clientID = process.env.client_id;

function searchChannels(string){
    query = 
    console.log(query);
    return query;
}

module.exports = {
    searchChannels:searchChannels
}